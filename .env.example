# ===== GCP / infra settings (required for Makefile & terraform auto-creation) =====
# MUST be the project ID (human name), NOT the numeric project number.
GCP_PROJECT_ID=<your-sandbox>
GCP_REGION=us-central1

# Optional override of service/repo names (defaults match repo)
SERVICE_NAME=smart-book-gist
ARTIFACT_REPO=gist-repo

# If you want to set image path manually (leave empty for first apply)
# Example full image: us-central1-docker.pkg.dev/sandbox-ahmed/gist-repo/smart-book-gist:latest
TF_IMAGE_TAG=

# ===== Application / Secret settings (required) =====
# Your Groq API key (do NOT commit .env)
GROQ_API_KEY=<your-groq-key>

# Optional model & endpoint
GROQ_API_URL=https://api.groq.com/openai/v1/chat/completions
GROQ_MODEL=openai/gpt-oss-20b

# ===== Local dev toggles (for Zscaler TLS handling) =====
# local  -> uses local project .certs bundle
# cloud  -> production behaviour (do not include Zscaler cert)
ENVIRONMENT=local

# Paths to your project-local combined cert bundle created by scripts/setup_certs.py
SSL_CERT_FILE=/home/wincn/projects/smart-book-gist/.certs/certifi_with_zscaler.pem
REQUESTS_CA_BUNDLE=/home/wincn/projects/smart-book-gist/.certs/certifi_with_zscaler.pem

# Output dir for local tool runs
OUTPUT_DIR=outputs
